<section class="offset-top-large">
    <div class="gc">
        <div class="d-4-10">
            <h1>{{ page.title }}</h1>
            <div>{{ page.content }}</div>
        </div>
    </div>
</section>

